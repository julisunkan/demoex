// SignInGate.tsx — deprecated. Auth is now handled directly in App.tsx.
// Kept as an empty placeholder to avoid import errors from any cached references.
export default function SignInGate() { return null; }
